# DEMS Frontend Environment Configuration Example
# Copy this file to .env for local development or set VITE_API_BASE_URL in Vercel project environment settings.
# Do not commit real production URLs, credentials, or JWT tokens to version control.

VITE_API_BASE_URL=http://localhost:8080
