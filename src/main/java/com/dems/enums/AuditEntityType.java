package com.dems.enums;

/**
 * Defines target domain entity types for audit event categorization in DEMS.
 */
public enum AuditEntityType {
    USER,
    CASE,
    EVIDENCE,
    CUSTODY,
    INTEGRITY,
    AUTHENTICATION,
    SYSTEM
}
